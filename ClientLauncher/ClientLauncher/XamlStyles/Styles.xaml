<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ==================== COLOR BRUSHES ==================== -->
    <SolidColorBrush x:Key="PrimaryColor" Color="#2A4D52"/>
    <SolidColorBrush x:Key="SecondaryColor" Color="#E1B7A7"/>
    <SolidColorBrush x:Key="AccentColor" Color="#3498DB"/>
    <SolidColorBrush x:Key="SuccessColor" Color="#27AE60"/>
    <SolidColorBrush x:Key="DangerColor" Color="#E74C3C"/>
    <SolidColorBrush x:Key="WarningColor" Color="#F39C12"/>
    <SolidColorBrush x:Key="TextPrimary" Color="#2C3E50"/>
    <SolidColorBrush x:Key="TextSecondary" Color="#7F8C8D"/>
    <SolidColorBrush x:Key="BackgroundLight" Color="#F5F5F5"/>
    <SolidColorBrush x:Key="BackgroundWhite" Color="White"/>
    <SolidColorBrush x:Key="BorderColor" Color="#BDC3C7"/>

    <!-- Header Gradient -->
    <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#1A2D30" Offset="0"/>
        <GradientStop Color="#274549" Offset="0.5"/>
        <GradientStop Color="#2A4D52" Offset="1"/>
    </LinearGradientBrush>

    <!-- ==================== EFFECTS ==================== -->
    <DropShadowEffect x:Key="CardShadow" 
                      BlurRadius="18" 
                      ShadowDepth="3" 
                      Opacity="0.15" 
                      Color="#274549"/>

    <DropShadowEffect x:Key="TextShadow" 
                      BlurRadius="10" 
                      ShadowDepth="2" 
                      Opacity="0.5" 
                      Color="Black"/>

    <DropShadowEffect x:Key="GlowEffect" 
                      Color="White" 
                      BlurRadius="12" 
                      ShadowDepth="0" 
                      Opacity="0.1"/>

    <!-- ==================== MODERN CHECKBOX STYLE ==================== -->
    <Style x:Key="ModernCheckBox" TargetType="CheckBox">
        <Setter Property="Width" Value="22"/>
        <Setter Property="Height" Value="22"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid>
                        <!-- Checkbox Border -->
                        <Border x:Name="CheckBoxBorder"
                                Width="22" 
                                Height="22"
                                Background="White"
                                BorderBrush="#BDC3C7"
                                BorderThickness="2"
                                CornerRadius="6">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="4" 
                                                  ShadowDepth="1" 
                                                  Opacity="0.1" 
                                                  Color="Black"/>
                            </Border.Effect>
                        </Border>

                        <!-- Check Mark -->
                        <Path x:Name="CheckMark"
                              Data="M 4,10 L 9,15 L 18,5"
                              Stroke="{StaticResource BackgroundWhite}"
                              StrokeThickness="3"
                              StrokeLineJoin="Round"
                              Opacity="0"
                              Margin="2"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!-- Checked State -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckBoxBorder" Property="Background" Value="{StaticResource PrimaryColor}"/>
                            <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                            <Setter TargetName="CheckMark" Property="Opacity" Value="1"/>
                        </Trigger>

                        <!-- Hover State (Unchecked) -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            <Setter TargetName="CheckBoxBorder" Property="Background" Value="#ECF0F1"/>
                        </MultiTrigger>

                        <!-- Hover State (Checked) -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="CheckBoxBorder" Property="Background" Value="#1F3C40"/>
                        </MultiTrigger>

                        <!-- Disabled State -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="CheckBoxBorder" Property="Background" Value="#ECF0F1"/>
                            <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#BDC3C7"/>
                            <Setter TargetName="CheckMark" Property="Stroke" Value="#95A5A6"/>
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== MODERN BUTTON STYLE ==================== -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryColor}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="18,10"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                            CornerRadius="8" 
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2" Color="Black"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#229954"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="#BDC3C7"/>
                <Setter Property="Foreground" Value="#7F8C8D"/>
                <Setter Property="Cursor" Value="No"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="Background" Value="{StaticResource TextSecondary}"/>
        <Setter Property="Foreground" Value="White"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#5D6D7E"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="Background" Value="{StaticResource DangerColor}"/>
        <Setter Property="Foreground" Value="White"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#C0392B"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="MinWidth" Value="100"/>
    </Style>

    <!-- ==================== SEARCH BOX STYLE ==================== -->
    <Style x:Key="ModernSearchBox" TargetType="Border">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="12,10"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SearchTextBox" TargetType="TextBox">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Style.Triggers>
            <Trigger Property="Text" Value="">
                <Setter Property="Background">
                    <Setter.Value>
                        <VisualBrush Stretch="None" AlignmentX="Left">
                            <VisualBrush.Visual>
                                <TextBlock Text="🔍 Search by name, code, category..." 
                                           Foreground="#95A5A6"
                                           FontStyle="Italic"
                                           FontSize="14"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ==================== VIEW MODE TOGGLE BUTTONS ==================== -->
    <Style x:Key="ViewModeToggleButton" TargetType="ToggleButton">
        <Setter Property="Width" Value="50"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="5">
                        <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ==================== CARD BORDER STYLE ==================== -->
    <Style x:Key="CardBorder" TargetType="Border">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="15"/>
        <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
    </Style>

    <Style x:Key="AppCardBorder" TargetType="Border">
        <Setter Property="Padding" Value="15"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Background" Value="#FAFAFA"/>
        <Setter Property="BorderBrush" Value="#E0E0E0"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#ECF0F1"/>
                <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                <Setter Property="BorderThickness" Value="2"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ==================== GROUP HEADER STYLE ==================== -->
    <GroupStyle x:Key="CategoryGroupStyle">
        <GroupStyle.HeaderTemplate>
            <DataTemplate>
                <Border Background="{StaticResource PrimaryColor}" 
                        CornerRadius="8" 
                        Padding="12,10" 
                        Margin="5,10,5,5">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2" Color="Black"/>
                    </Border.Effect>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📁" 
                                   FontSize="18" 
                                   Margin="0,0,10,0"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Name}" 
                                   FontSize="18" 
                                   FontWeight="Bold"
                                   Foreground="White"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding ItemCount, StringFormat=' ({0} Apps)'}" 
                                   FontSize="15" 
                                   Foreground="{StaticResource SecondaryColor}"
                                   Margin="8,0,0,0"
                                   VerticalAlignment="Center"
                                   FontWeight="Medium"/>
                    </StackPanel>
                </Border>
            </DataTemplate>
        </GroupStyle.HeaderTemplate>
    </GroupStyle>

    <!-- ==================== BADGE STYLES ==================== -->
    <Style x:Key="CategoryBadge" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource DangerColor}"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="CornerRadius" Value="5"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
    </Style>

    <Style x:Key="CodeBadge" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource TextSecondary}"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="CornerRadius" Value="5"/>
    </Style>

    <Style x:Key="UpdateBadge" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource WarningColor}"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="CornerRadius" Value="5"/>
        <Setter Property="Margin" Value="8,0,0,0"/>
    </Style>

    <!-- ==================== TAB VIEW ITEM TEMPLATE ==================== -->
    <DataTemplate x:Key="TabViewItemTemplate">
        <Border Style="{StaticResource AppCardBorder}" 
                Width="280" 
                Height="200"
                Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header with Checkbox -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                              Style="{StaticResource ModernCheckBox}"
                              HorizontalAlignment="Left"/>

                    <Border Background="{StaticResource WarningColor}"
                            Padding="6,3"
                            CornerRadius="4"
                            HorizontalAlignment="Right"
                            Visibility="{Binding HasUpdate, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="🆕 UPDATE" 
                                   FontSize="10" 
                                   Foreground="White"
                                   FontWeight="Bold"/>
                    </Border>
                </Grid>

                <!-- Icon and Info -->
                <StackPanel Grid.Row="1" 
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Center">
                    <Border Width="80" 
                            Height="80" 
                            Background="Transparent" 
                            CornerRadius="15"
                            Margin="0,0,0,15">
                        <Image Stretch="Uniform">
                            <Image.Source>
                                <MultiBinding Converter="{StaticResource IconConverter}">
                                    <Binding Path="IconUrl"/>
                                    <Binding Path="Category"/>
                                </MultiBinding>
                            </Image.Source>
                        </Image>
                    </Border>

                    <TextBlock Text="{Binding Name}" 
                               FontSize="16" 
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimary}"
                               TextAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>

                <!-- Status -->
                <TextBlock Grid.Row="2"
                           Text="{Binding StatusText}" 
                           FontSize="11" 
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Margin="0,10,0,10"
                           MaxHeight="35">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsInstalled}" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource SuccessColor}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsInstalled}" Value="False">
                                    <Setter Property="Foreground" Value="{StaticResource DangerColor}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding HasUpdate}" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource WarningColor}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Action Button -->
                <Button Grid.Row="3"
                        Content="🗑️ Uninstall"
                        Style="{StaticResource DangerButton}"
                        Command="{Binding DataContext.UninstallCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        Visibility="{Binding IsInstalled, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Padding="10,6"
                        FontSize="12"/>
            </Grid>
        </Border>
    </DataTemplate>

    <!-- ==================== LIST VIEW ITEM TEMPLATE ==================== -->
    <DataTemplate x:Key="ListViewItemTemplate">
        <Border Style="{StaticResource AppCardBorder}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Checkbox -->
                <CheckBox Grid.Column="0" 
                          IsChecked="{Binding IsSelected, Mode=TwoWay}"
                          Style="{StaticResource ModernCheckBox}"
                          VerticalAlignment="Center"
                          Margin="0,0,15,0"/>

                <!-- Icon -->
                <Border Grid.Column="1" 
                        Width="60" 
                        Height="60" 
                        Background="Transparent" 
                        CornerRadius="10"
                        VerticalAlignment="Center"
                        Margin="0,0,15,0">
                    <Image Stretch="Uniform">
                        <Image.Source>
                            <MultiBinding Converter="{StaticResource IconConverter}">
                                <Binding Path="IconUrl"/>
                                <Binding Path="Category"/>
                            </MultiBinding>
                        </Image.Source>
                    </Image>
                </Border>

                <!-- Info -->
                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Name}" 
                               FontSize="18" 
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimary}"/>

                    <TextBlock Text="{Binding Description}" 
                               FontSize="13" 
                               Foreground="{StaticResource TextSecondary}"
                               TextWrapping="Wrap"
                               Margin="0,5,0,0"
                               MaxHeight="40"/>

                    <!-- Status -->
                    <TextBlock Text="{Binding StatusText}" 
                               FontSize="13" 
                               FontWeight="SemiBold"
                               TextWrapping="Wrap"
                               Margin="0,8,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsInstalled}" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource SuccessColor}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsInstalled}" Value="False">
                                        <Setter Property="Foreground" Value="{StaticResource DangerColor}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding HasUpdate}" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource WarningColor}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Badges -->
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Border Style="{StaticResource CategoryBadge}">
                            <TextBlock Text="{Binding Category}" 
                                       FontSize="11" 
                                       Foreground="White"
                                       FontWeight="SemiBold"/>
                        </Border>

                        <Border Style="{StaticResource CodeBadge}">
                            <TextBlock Text="{Binding AppCode}" 
                                       FontSize="11" 
                                       Foreground="White"
                                       FontWeight="SemiBold"/>
                        </Border>

                        <Border Style="{StaticResource UpdateBadge}"
                                Visibility="{Binding HasUpdate, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="🆕 UPDATE" 
                                       FontSize="10" 
                                       Foreground="White"
                                       FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </StackPanel>

                <!-- Uninstall Button -->
                <Button Grid.Column="3"
                        Content="🗑️ Uninstall"
                        Style="{StaticResource DangerButton}"
                        Command="{Binding DataContext.UninstallCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        Visibility="{Binding IsInstalled, Converter={StaticResource BooleanToVisibilityConverter}}"
                        VerticalAlignment="Center"
                        FontSize="12"
                        Padding="15,10"/>
            </Grid>
        </Border>
    </DataTemplate>

</ResourceDictionary>